export class Book {
    constructor(
        id: string,
        title: string,
        author: string,
        description: string,
        invoice: string,
        unique_code: string,
        img_uer: string,
        loan_status: boolean
    ){}
    
    // Otros campos si es necesario

    // Puedes agregar lógica relacionada con el libro aquí
}