export class Loan {
    constructor(
        uuid: string,
        uuid_book: string,
        uuid_user: string,
        loan_date: string,
        dedline: string,
        status: boolean
    ) {
        
    }
}