export class Review {
    constructor(
        id: string,
        id_user: string,
        id_book: string,
        loan_date: Date,
        deadline: Date,
        status: boolean,
    ) {
        
    }
}